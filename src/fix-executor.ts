/**
 * THIS FILE IS DEPRECATED AND NO LONGER USED
 * 
 * The TEST LIVE VIEW section has been removed from the extension.
 * This file is kept for reference purposes only and will be removed in a future update.
 */

import * as vscode from 'vscode';

/**
 * This function previously registered the TEST LIVE VIEW provider, which has been removed
 * Kept as a stub to prevent import errors
 * @param context The extension context
 * @returns void
 */
export function registerTestLiveView(context: vscode.ExtensionContext): any {
    console.warn('TEST LIVE VIEW has been removed from the extension');
    return {}; // Return empty object to satisfy any code that might still use this
}
